#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
        // sleep(10);
        printf("Hola Mundo\n");

        if (1 < 2){
          printf("Que esperabas, parguela");
        }
        else{
          printf("No me lo creo");
        }
        
        return 0;
}

// tiene un tipo de salida int -> return 0
// sin "int argc, char *argv[]" no puede tomar parámetros
// int argc: numero de parámetros
// -o object
